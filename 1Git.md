# Day 1 #

bare repo means has no project files only git hsitory and metadata

```
yum install git -y 

git --version
mkdir -p /opt/news.git   # parent directory if doesnt exit

sudo git init --bare /opt/news.git

```

